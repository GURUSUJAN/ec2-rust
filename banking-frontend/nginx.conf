server {
    listen 80;
    
    # Serve HTML/CSS/JS
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Proxy /api requests to the Rust container
    location /api/ {
        proxy_pass http://backend:8080/api/; 
    }
}